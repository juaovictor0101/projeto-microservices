server:
  port: 8082

spring:
  application:
    name: microservice-pedidos
  datasource:
    url: jdbc:postgresql://localhost:5432/pedidodb
    username: dbjaum
    password: "1101"
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/