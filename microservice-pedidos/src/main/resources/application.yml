server:
  port: 8083

spring:
  application:
    name: microservice-pedidos
  datasource:
    url: jdbc:postgresql://postgres:5432/pedidodb
    username: dbjaum
    password: "1101"
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://discovery:8761/eureka/