server:
  port: 8084

spring:
  application:
    name: microservice-produtos
  datasource:
    url: jdbc:postgresql://postgres:5432/produtodb
    username: dbjaum
    password: "1101"
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://discovery:8761/eureka/